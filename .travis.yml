# Building a Java project
language: java

# We can specify a list of JDKs to be used for testing
jdk:
 - openjdk11
after_success:
  - bash <(curl -s https://codecov.io/bash)
notifications:
 email:
   - 2758972837@qq.com(replace by your e-mail)
